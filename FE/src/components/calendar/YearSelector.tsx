import React from 'react';
import {StyleSheet, View} from 'react-native';

interface YearSelectorProps {}

const YearSelector = ({}: YearSelectorProps) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default YearSelector;
